<ssl-decrypt>
        <trusted-root-CA>
          <member>{{ trusted_root_ca_name }}</member>
        </trusted-root-CA>
        <forward-trust-certificate>
          <rsa>{{ trusted_root_ca_name }}</rsa>
        </forward-trust-certificate>
        <forward-untrust-certificate>
          <rsa>{{ trusted_root_ca_name }}</rsa>
        </forward-untrust-certificate>
      </ssl-decrypt>